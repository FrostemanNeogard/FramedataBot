services:
  bot:
    build: .
    volumes:
      - /usr/src/app
    command: npm run dev
    environment:
      - BASE_API_URL=${BASE_API_URL}
    networks:
      - framedata_network
    restart: always

networks:
  framedata_network:
    external: true
